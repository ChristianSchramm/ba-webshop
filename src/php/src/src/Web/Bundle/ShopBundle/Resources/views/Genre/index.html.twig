{% extends 'WebShopBundle::admin.html.twig' %}

{% block title %} Admin Control Panel{% endblock %}

{% block content %}

<h2 class="heading style1">Genre Verwalten</h2>
<a class="btn new-btn" href="{{ path('admin_genre_new') }}">Neuen Genre anlegen</a>

                        <table cellpadding="0" cellspacing="0">
                          <thead>
                            <tr>
                              <th><h3 class="heading style3">Bezeichnung</h3></th>
                              <th><h3 class="heading style3">Löschen</h3></th>
                            </tr>
                          </thead>
                          <tbody>
                            {% for genre in genres %}
                            <tr>
                              <td><p>{{ genre.name }}</p></td>
                              <td><a class="delete" href="{{ path('admin_genre_remove', { 'id': genre.id}) }}">X</a></td>

                            </tr>
                            {% endfor %}
                          </tbody>
                          <tfoot>
                            <tr>
                              <td colspan="4"></td>
                            </tr>
                          </tfoot>
                        </table>




{% endblock %}