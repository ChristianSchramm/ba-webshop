{% extends '::base.html.twig' %}

{% block body %}

<h2>Users</h2>
<ul>
    {% for user in users %}
        <li>{{ user.username }}
          <a href="{{ path('admin_user_bill', {'id': user.id}) }}">Bestellungen</a>
        </li>
    {% endfor %}
</ul>

{% endblock %}